num_rounds = int(input("Enter your number rounds : "))

# TODO: Your code here
# Use input() inside the loop to get each round's score
num_rounds = 0
round_processed = 0

for i in range(num_rounds):
    print()


    if(round_processed <= 100):
        point = round_processed
    else:
        point = round_processed + (round_processed * 0.2)

    


print(f"{final_score:.1f}")
print(rounds_processed)